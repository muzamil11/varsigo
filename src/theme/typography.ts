// Font-size scale used across the app. NativeWind classes cover most cases;
// use these when a component needs a raw numeric size (e.g. vector icons).
export const fontSize = {
  xs: 12,
  sm: 14,
  base: 16,
  lg: 18,
  xl: 20,
  '2xl': 24,
  '3xl': 30,
} as const;
